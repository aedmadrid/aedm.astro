---
import Default from "../../layouts/Default.astro";
import NotionPage from "../../components/NotionPage.astro";

export function getStaticPaths() {
    return [];
}

const { pageId } = Astro.params;
---

<Default title="" header={false}>
    <NotionPage pageId={pageId!} />
</Default>
